# Notes

Short Markdown summaries, one file per topic (e.g. `tensor-products.md`, `phase-kickback.md`).
If a file already exists, add your own section with your name as a heading instead of creating a duplicate.
